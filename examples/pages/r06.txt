uehaj herdr-jev-prompt-router: 273-line, zero-dependency TypeScript router. Asks Jev ONE Choice question to decide which of 5-10 open herdr workspaces a prompt should go to, then places the text in that pane's input (does NOT press Enter — human keeps the send decision). "Make criteria keys machine IDs and values the LLM-facing description; the answer comes back usable as a key directly."
