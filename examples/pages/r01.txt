pi-jev-model-router v0.2.0: One Jev request with 4 parallel questions before every turn: task_kind (Choice), complexity (Score), capability_deserved (Score), needs_deep_reasoning (Noul). Then: demand = 0.55*complexity + 0.45*capability (+ reasoning nudge); demand = max(demand, kind floor) # planning/review never go cheap; confidence guard -> budget guard -> availability guard -> cache guard. "Jev judges the task, code owns the budget." Daily/monthly spend caps auto-downgrade tiers; fails open on missing key/timeout.
