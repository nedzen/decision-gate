aidxn.com — cheap models as always-on watchers: "The expensive model does the thinking, the cheap model does the staring." tail -f dev.log | grep -Ei 'error|failed', then a small model reads the matches and answers one question: did anything actually explode. A Haiku-class model does it perfectly and costs a rounding error to leave on all day. Wire it as a notification, not a report.
